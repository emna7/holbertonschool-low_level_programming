#include"holberton.h"
/**
 * _strcmp - function that compares two strings
 * @s1: pointer
 * @s2: pointer
 * Return: 0
*/
int _strcmp(char *s1, char *s2)
{
int i, j, x;
i = 0;
j = 0;
while (s1[i] != '\0')
i++;
while (s2[j] != '\0')
j++;
for (i < j || i = j || i > j)
_putchar(x = i - j + '0');
return (x);
}
